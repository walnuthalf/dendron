import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`

`;

const Channel = () => (
  <Wrapper>
    Hello thats's message
  </Wrapper>
);

export default Channel;
